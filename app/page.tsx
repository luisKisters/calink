export default function Home() {
  return (
    <main className="shell">
      <section>
        <p className="eyebrow">Calink backend</p>
        <h1>Thin test surface</h1>
        <p>
          The production UI is intentionally out of scope for this phase. Use
          Convex functions and HTTP endpoints to exercise the backend.
        </p>
      </section>
    </main>
  );
}
